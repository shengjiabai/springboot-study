1.cup负载高负载特别高时，任务就会排队，不能及时执行
2.uptime命令可以直接查看负载
3.iostat命令io使用情况
4.零拷贝
5.缓冲区性能优化
6.MySQL 的 InnoDB 引擎，通过配置合理的 innodb_buffer_pool_size，减少换页，增加数据库的性能。
7.因此在以下场景下G1更适合：
    1）服务端多核CPU、JVM内存占用较大的应用。
    2）应用在运行过程中会产生大量内存碎片、需要经常压缩空间。
    3）想要更可控、可预期的GC停顿周期；防止高并发下应用雪崩现象。
8。连接池的大小设置得越大越好，这是一种误解。根据经验，数据库连接，只需要 20~50 个就够用了。具体的大小，要根据业务属性进行调整，但大得离谱肯定是不合适的。